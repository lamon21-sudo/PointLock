-- This migration was partially applied and manually resolved
-- The database already has the new columns from partial execution
-- Marking as empty migration to sync state

-- Migration completed manually
SELECT 1;
